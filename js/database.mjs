export class Database {
  constructor() {}

  /**
   * create conncetion with database
   */
  conncetionDB() {
    //if no conn -> new conn
    //else nothing to do
    console.log("Hier shoud be connection with DB");
  }
}
